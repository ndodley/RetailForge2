Place shared fallback images in this folder.

For this project, copy the original RetailForge dummy image here as:

- dummy_product.jpg

Then the default product image path `/images/other_images/dummy_product.jpg` will resolve through Spring Boot's media resource handler.

